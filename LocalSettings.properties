#################################################################### 
# Settings for local Kettle Jobs
# The following settings are used only by the current ETL job
#
# We only have here flag available also in global Settings.properties
# The job will first load variables from global Settings.properties
# then when each job is running it will overwrite the value defined in global
#
##################################################################### 


#
# Check this flag to connect/ignore external interfaces
# It will not send or retrieve for example files to/from external SFTP
# This is used for test when you don't have connectivity
#
FLAG_CONNECT_TO_EXTERNAL_INTERFACES=N

